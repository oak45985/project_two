const path = require('path')

module.exports = {
  'config': path.resolve('config/config.json'),
  'models': path.resolve('models'),
  'migrations': path.resolve('db/migrate'),
  'seeders': path.resolve('db/seeders')
}